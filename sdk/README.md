# Tiwut API SDKs

This folder contains language bindings and client interface definitions for integrating with Tiwut API.

---

## SDK Modules

- **`cfeel/`**: Foreign Function Interface (FFI) bindings for `.cfeel` programs.
- **`cpp/`**: C++ header interface for native C++17 projects leveraging `CPP-Headers`.
